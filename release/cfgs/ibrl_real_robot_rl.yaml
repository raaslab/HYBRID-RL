episode_length: 300
stddev_max: 0.1
# bc_policy: "assembly"
preload_num_data: 5
env_reward_scale: 1
num_train_step: 40000
replay_buffer_size: 500
use_wb: 0
num_eval_episode: 20
q_agent:
  act_method: "ibrl"
  # enc_type: "drq"
  enc_type: "resnet"
  actor:
    dropout: 0
    hidden_dim: 1024
    feature_dim: 64
  critic:
    feature_dim: 64
